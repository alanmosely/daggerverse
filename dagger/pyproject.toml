[project]
name = "main"
version = "0.1.0"
description = "Repo-level Dagger module for GitHub release automation"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
